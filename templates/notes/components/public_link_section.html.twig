{#
  PublicLinkSection Component
  Displays current public URL with copy action
  Props: publicUrl (string), urlToken (string)
#}

{% if urlToken is defined and urlToken is not empty %}
<div class="mt-6">
    <div class="flex items-start justify-between gap-4 mb-4">
        <div>
            <p class="text-lg font-bold text-gray-900 mb-1">🔗 Publiczny link</p>
            <p class="text-sm text-gray-600 font-medium">
                Każdy, kto ma ten link, może zobaczyć notatkę (jeżeli notatka jest publiczna).
            </p>
        </div>
    </div>
    <div class="flex flex-wrap items-center gap-3 p-4 bg-gradient-to-r from-indigo-50 to-purple-50 rounded-xl border border-indigo-200">
        <a href="{{ publicUrl }}" class="font-mono text-sm text-indigo-700 hover:text-indigo-900 break-all underline decoration-2 underline-offset-4 font-semibold flex-1" target="_blank" rel="noopener noreferrer" data-public-link>
            {{ publicUrl }}
        </a>
        <button type="button" data-copy-public-link data-link="{{ publicUrl }}" class="btn-gradient inline-flex items-center gap-2 px-5 py-3 text-sm font-bold text-white rounded-xl shadow-lg hover:shadow-2xl whitespace-nowrap">
            📋 Kopiuj
        </button>
    </div>
</div>
{% endif %}
