{#
  Notes Layout Component
  Common layout, styles and structure for notes pages (edit, new)
  Props:
    - subtitle (string) - subtitle for the header
    - currentUserEmail (string) - email of current user
    - backUrl (string) - URL for back button
    - content (string) - HTML content to render
#}

<main class="relative min-h-screen pb-32 bg-gradient-to-br from-indigo-50 via-purple-50 to-cyan-50">
    <div aria-live="polite" class="sr-only" data-global-aria-live></div>
    <div id="toast-stack" class="toast-stack" aria-live="polite"></div>

    <!-- Sticky Glass Header -->
    <div class="sticky top-0 z-40 bg-white/85 backdrop-blur-2xl border-b border-white/30">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 py-3 sm:py-4">
            <header class="flex items-center justify-between gap-4">
                <a href="/notes" class="inline-flex items-center gap-2 sm:gap-3 group flex-shrink-0">
                    <div class="relative">
                        <div class="absolute inset-0 bg-gradient-to-br from-purple-500 to-indigo-600 rounded-xl sm:rounded-2xl blur-md opacity-75 group-hover:opacity-100 transition-opacity"></div>
                        <div class="relative inline-flex h-10 w-10 sm:h-12 sm:w-12 items-center justify-center rounded-xl sm:rounded-2xl text-white font-black text-lg sm:text-xl shadow-xl bg-gradient-to-br from-purple-500 to-indigo-600 transform group-hover:scale-110 transition-transform">
                            S
                        </div>
                    </div>
                    <div class="leading-tight hidden xs:block">
                        <div class="text-lg sm:text-xl font-bold bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent">Snipnote</div>
                        <div class="text-xs sm:text-sm text-indigo-600 font-medium">{{ subtitle ?? 'Notatki' }}</div>
                    </div>
                </a>
                
                <div class="flex items-center gap-2 sm:gap-4 text-xs sm:text-sm font-semibold">
                    <a href="{{ backUrl ?? '/notes' }}" class="px-3 sm:px-4 py-2 rounded-xl bg-white/60 text-indigo-600 hover:bg-white/80 backdrop-blur-sm border border-indigo-200/50 transition-all">
                        <span class="sm:hidden">←</span>
                        <span class="hidden sm:inline">← Powrót</span>
                    </a>
                    <div class="px-3 sm:px-4 py-2 rounded-xl bg-gradient-to-r from-indigo-500 to-purple-600 text-white shadow-lg truncate max-w-[120px] sm:max-w-[200px]">
                        {{ currentUserEmail ?? 'użytkownik' }}
                    </div>
                </div>
            </header>
        </div>
    </div>

    <div class="max-w-6xl mx-auto px-4 sm:px-6 py-6 sm:py-8 space-y-6">
        {{ content|raw }}
    </div>
</main>