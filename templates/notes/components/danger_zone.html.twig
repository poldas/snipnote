{#
  DangerZone Component
  Props: deleteUrl (string), regenerateUrl? (string|null), redirectUrl (string)
#}
<section class="bg-white/95 backdrop-blur-2xl rounded-2xl p-6 border-2 border-red-300 shadow-xl bg-gradient-to-br from-red-50 to-pink-50" data-danger-zone data-delete-url="{{ deleteUrl }}" data-redirect-url="{{ redirectUrl }}"{% if regenerateUrl is defined and regenerateUrl %} data-regenerate-url="{{ regenerateUrl }}"{% endif %}>
    <div class="mb-5">
        <h2 class="text-xl font-bold text-red-700 mb-1">🗑️ Strefa zagrożenia</h2>
        <p class="text-sm text-red-600 font-semibold">Operacje nieodwracalne. Używaj ostrożnie.</p>
    </div>
    <button type="button" data-open-delete-modal tabindex="6" class="inline-flex items-center gap-3 px-6 py-3 bg-gradient-to-r from-red-500 to-pink-500 text-white rounded-xl font-bold text-sm shadow-lg hover:shadow-2xl hover:scale-105 transition-all">
        ⚠️ Usuń notatkę
    </button>
</section>


