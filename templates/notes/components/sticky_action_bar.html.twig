{#
  StickyActionBar Component
  Bottom sticky bar with Save action
  Props:
    - saveLabel (string, optional)
#}
{% set saveLabel = saveLabel ?? 'Zapisz notatkę' %}

{#
  StickyActionBar Component
  Floating Save button in bottom right
#}
{% set saveLabel = saveLabel ?? 'Zapisz notatkę' %}

<div class="fixed bottom-6 right-4 sm:right-8 z-50 flex items-center gap-3">
    <div class="flex flex-col items-end gap-1 pointer-events-none">
        <span data-status-indicator class="text-xs text-indigo-900 font-bold bg-white/80 backdrop-blur-sm px-2 py-1 rounded-lg shadow-sm opacity-0 transition-opacity"></span>
        <span data-saving-spinner class="hidden bg-white/80 backdrop-blur-sm p-2 rounded-full shadow-md">
            <svg class="animate-spin h-5 w-5 text-indigo-600" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
        </span>
    </div>
    <button type="button" data-submit-btn class="btn-gradient flex items-center gap-2 sm:gap-3 px-5 sm:px-7 py-3 sm:py-4 text-white rounded-2xl font-black text-sm sm:text-base shadow-2xl hover:scale-110 active:scale-95 transition-all">
        <span class="text-xl sm:text-2xl">💾</span>
        <span class="hidden xs:inline">{{ saveLabel }}</span>
        <span class="xs:hidden">Zapisz</span>
    </button>
</div>

