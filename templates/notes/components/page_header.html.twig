{#
  Page Header Component
  Common header for note pages with title, subtitle and status badges
  Props:
    - title (string) - page title
    - subtitle (string) - page subtitle
    - statusBadges (array) - array of badge objects {text, type, icon}
#}
<div class="px-8 py-6 bg-gradient-to-r from-indigo-50 to-purple-50 border-b border-indigo-100/50">
    <div class="flex flex-wrap items-center justify-between gap-4">
        <div>
            <h1 class="text-3xl font-black gradient-text mb-2">{{ title }}</h1>
            <p class="text-sm text-gray-600 font-medium">{{ subtitle }}</p>
        </div>
        {% if statusBadges is defined and statusBadges %}
        <div class="flex items-center gap-3">
            {% for badge in statusBadges %}
            <span class="inline-flex items-center gap-2 px-4 py-2 rounded-full
                {% if badge.type == 'id' %}bg-white shadow-md border border-gray-200 text-sm font-bold text-gray-700{% endif %}
                {% if badge.type == 'visibility' and badge.icon == '🌐' %}bg-gradient-to-r from-green-400 to-emerald-500 text-white shadow-lg text-sm font-bold{% endif %}
                {% if badge.type == 'visibility' and badge.icon == '🔒' %}bg-gradient-to-r from-gray-400 to-slate-500 text-white shadow-lg text-sm font-bold{% endif %}
                {% if badge.type == 'new' %}bg-white shadow-md border border-gray-200 text-sm font-bold text-gray-700{% endif %}">
                {% if badge.icon %}<span class="w-2 h-2 {% if badge.type == 'id' %}bg-gray-400{% else %}bg-white{% endif %} rounded-full animate-pulse"></span>{% endif %}
                {% if badge.icon %}<span class="text-lg">{{ badge.icon }}</span>{% endif %}
                {{ badge.text }}
            </span>
            {% endfor %}
        </div>
        {% endif %}
    </div>
</div>
